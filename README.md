# 🚀 Тестовый проект

Мини-платформа для демонстрации трёх заданий:

1. ✅ **Генератор уникальных случайных последовательностей** 
2. 🌦 **Консольное приложение для получения погоды**  
3. 🧮 **Класс матриц без использования numpy**

Проект развёрнут в контейнере Docker и содержит веб-API, терминал ttyd и CLI-утилиту.

---

## 📦 Установка и запуск

### 🔹 1. Сборка Docker-образа
```bash
docker build -t maximum:latest .
```

### 🔹 2. Запуск контейнера
```bash
docker run -d --name maximum   -p 8000:8000 -p 7681:7681   -e TTYD_USER=demo   -e TTYD_PASS='StrongPass!123'   maximum:latest
```

### 🔹 3. Проверка статуса
```bash
docker ps
curl http://localhost:8000/healthz
```

---

## 🌐 Веб-интерфейсы

| Сервис | Адрес | Назначение |
|--------|--------|-------------|
| **Главная страница** | [https://maximum.gorbachx.ru](https://maximum.gorbachx.ru) | Главная страница тестового задания |
| **FastAPI** | [https://maximum.gorbachx.ru/docs](https://maximum.gorbachx.ru/docs) | Swagger UI для тестирования API |
| **Terminal (ttyd)** | [https://maximum.gorbachx.ru/term](https://maximum.gorbachx.ru/term) | Веб-консоль внутри контейнера |

**Логин:** `demo`  
**Пароль:** `В ЛС ТГ`

---

## ⚙️ 1. Генератор уникальных ID

### 📍 Эндпоинт
`POST /generate`

### 🔹 Пример запроса
```json
{
  "length": 8,
  "count": 3
}
```

### 🔹 Пример ответа
```json
{
  "items": ["aZ8kP2qW", "m3D9yTrL", "q9h7S1bJ"],
  "length": 8,
  "count": 3
}
```

- `length` — длина строки  
- `count` — количество строк  
- алфавит по умолчанию: **[a-zA-Z0-9]**

### 🔹 Быстрый тест из терминала
```bash
curl -X POST http://localhost:8000/generate   -H "Content-Type: application/json"   -d '{"length": 10, "count": 5}'
```

---

## 🌦 2. CLI: Погода по городу

### 📍 Пример запуска
```bash
python3 cli/get_weather_data.py Moscow
```

### 🔹 Пример вывода
```
Moscow, Moscow City, Russia
Now: 7°C (feels 5°C), Partly cloudy; Wind 15 km/h, Humidity 70%
```

---

## 🧮 3. Класс Matrix (базовая линейная алгебра)

Файл: `libs/matrix.py`

Поддерживает операции:
- сложение (`A + B`)
- вычитание (`A - B`)
- умножение (`A * B`)
- деление (`A / B`) — умножение на обратную матрицу
- вычисление детерминанта (`A.determinant()`)

### 📍 Пример использования
```bash
python3 - <<'PY'
from libs.matrix import Matrix

A = Matrix([[1, 2], [3, 4]])
B = Matrix([[5, 6], [7, 8]])

print("A + B =", A + B)
print("A * B =", A * B)
print("det(A) =", A.determinant())
print("A / B =", A / B)
PY
```

---

## 🧰 Структура проекта

```
maximum/
├── app.py                 # FastAPI-приложение (генератор ID)
├── cli/
│   └── get_weather_data.py  # CLI-утилита для погоды
├── libs/
│   └── matrix.py            # Класс Matrix
├── supervisord.conf       # Запуск uvicorn + ttyd
├── start.sh               # Точка входа контейнера
├── Dockerfile             # Описание образа
└── README.md              # Документация
```

---

## 🧠 Примечания

- Все операции выполняются без сторонних библиотек (`numpy`, `requests`, `pandas` и т.п.).  
- Контейнер использует `supervisord` для управления процессами:
  - `uvicorn` (FastAPI)
  - `ttyd` (веб-терминал)
- Проект создан для демонстрации.

---

## 🧑‍💻 Автор

**Михаил Горбач**  

